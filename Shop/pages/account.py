import flet as ft
def AccountPage(page: ft.Page):
    page.add(
            ft.SafeArea(
                ft.Column(
                    [
                        ft.Text("Mi cuenta", size=30),
                    ]
                )
            )
        )